<%= form_for(chapter) do |f| %>
  <% if chapter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chapter.errors.count, "error") %> prohibited this chapter from being saved:</h2>

      <ul>
      <% chapter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_area :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <%= f.hidden_field :subject_id, value: params[:subject_id] %>
<!--   <div class="field">
    <%#= f.label :subject_id %>
    <%#= f.select :subject_id, options_for_select(Book.all.collect{ |b| [b.title, b.id] }, chapter.subject_id) %>
  </div> -->

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
